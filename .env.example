# Contact form (Formspree).
# The form posts directly to the Formspree endpoint defined in lib/site.ts.
# The form ID is public by design, so this is optional — only set it to point
# the form at a different Formspree form without editing code.
# NEXT_PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/moeanjvz
